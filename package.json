{
  "name": "shippment-tracker",
  "version": "1.0.0",
  "description": "Shipment tracking API",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx @puppeteer/browsers install chromium@stable --path=/opt/render/.cache/puppeteer --force"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "express": "^4.19.2",
    "puppeteer-core": "^24.16.2"
  }
}
